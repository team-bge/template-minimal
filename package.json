{
  "name": "template-minimal",
  "description": "A BGE game template with only the essentials",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "bge build",
    "start": "bge serve"
  },
  "devDependencies": {
    "bge-dev": "^0.3.8"
  },
  "dependencies": {
    "bge-core": "^0.3.12"
  }
}
